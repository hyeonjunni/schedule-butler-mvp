# Agent Collaboration Guide

이 저장소는 여러 코딩 에이전트가 같은 목표를 공유하도록 문서 중심으로 시작합니다. 답변과 작업 요약은 한국어로 작성합니다.

## 목표

3일 안에 Schedule Butler MVP를 구현합니다.

성공 기준:

- 정제되지 않은 카톡/이메일/STT 텍스트를 입력할 수 있다.
- AI가 일정, TODO, 체크리스트, 참석자, 시간 제약을 구조화한다.
- 일정 상태를 `confirmed_event`, `negotiating_event`, `needs_more_info`로 분류한다.
- 등록 전 승인 대화가 반드시 나온다.
- 승인 후 DB와 가상 캘린더에 저장된다.
- 일정 30분 전 알림과 출발 전 체크리스트가 생성된다.

## 작업 원칙

- `.env` 또는 실제 API key, SSH password, DB password를 절대 커밋하지 않는다.
- 새 기능보다 MVP 성공 기준을 우선한다.
- 큰 리팩터링을 피하고, 변경 목적이 명확한 파일만 수정한다.
- 구현 전후로 실행 가능한 검증 방법을 남긴다.
- 애매한 일정은 저장하지 말고 제안 또는 추가 질문으로 보낸다.

## 추천 스택

- Frontend/Backend: Next.js
- UI: 모바일 우선 iPhone 스타일 웹 UI
- DB: PostgreSQL
- ORM: Prisma
- AI: OpenAI API 우선, 이후 로컬 LLM 교체 가능하게 provider 인터페이스 분리

## 핵심 도메인 규칙

정제되지 않은 입력은 바로 일정으로 확정하지 않습니다.

- 날짜/시간/제목이 충분하면 승인 후 등록합니다.
- 가능한 시간이 오가는 회의는 공통 가능 시간을 계산한 뒤 후보를 제안합니다.
- 공통 시간이 없거나 충돌하면 사용자에게 보낼 확인 메시지 초안을 만듭니다.
- 사용자 승인 없이 `Event`를 생성하지 않습니다.

## 권장 작업 순서

1. Next.js 앱과 모바일 UI 골격 생성
2. AI 추출 API route 구현
3. 추출 JSON schema 검증
4. 승인 대화 UI 구현
5. Prisma schema와 PostgreSQL 연결
6. 캘린더/TODO/체크리스트 화면 구현
7. 알림 생성 로직 구현
8. 샘플 원문 테스트 추가

## 금지

- `.env` 읽은 값을 로그나 문서에 출력하지 않기
- 사용자 승인 없이 자동 등록하지 않기
- 실제 카카오톡/이메일/Apple Calendar 연동을 MVP에 끼워 넣지 않기
- 이미지 OCR, 음성 STT를 1차 범위에 넣지 않기
